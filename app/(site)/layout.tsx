import SiteRouteLayout from '@/components/studio-jamila/SiteRouteLayout';

export default function SiteLayout({ children }: { children: React.ReactNode }) {
  return <SiteRouteLayout>{children}</SiteRouteLayout>;
}
